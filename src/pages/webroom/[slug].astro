---
import VideoRoom from '@widgets/Screens/WebRoom'
import Layout from 'src/layouts/Layout.astro'
import { Cursor } from '@shared/ui/Cursor/index'
import { URL } from 'url'

import { Loader } from '@shared/ui/Loader/index'
import '@shared/styles/global.scss'
import '@shared/styles/scrollbar.scss'

export const prerender = false
const roomId = Astro.params.slug

import styles from './styles/services/styles.module.scss'
---

<Layout title="Игра">
	<Cursor client:load />
	<!-- <Loader client:load /> -->
	<main class="h-full w-full bg-primary-dark">
		<VideoRoom client:load roomId={roomId} />
	</main>
</Layout>
